name: Deploy to cPanel

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: |
          echo "FTP deployment has been removed from this workflow."
          echo "Configure your own deployment step if needed."
